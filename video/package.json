{
  "name": "now-in-korea-video",
  "version": "1.0.0",
  "private": true,
  "description": "트렌드 요약을 쇼츠(세로) 영상으로 렌더링하는 Remotion 프로젝트. 메인 서버(practice)와는 별개로 여기서만 필요한 의존성을 쓴다.",
  "scripts": {
    "prepare-data": "node src/prepare-data.js",
    "generate-script": "node src/generate-script.js",
    "generate-image": "node src/generate-image.js",
    "generate-audio": "node src/generate-audio.js",
    "studio": "remotion studio src/index.js",
    "debug-stills": "node src/debug-stills.js",
    "youtube-auth": "node src/youtube-auth.js",
    "upload": "node src/upload-youtube.js",
    "render": "npm run prepare-data && npm run generate-script && npm run generate-image && npm run generate-audio && node src/render-all.js"
  },
  "dependencies": {
    "@google/genai": "2.20.0",
    "@remotion/bundler": "4.0.520",
    "googleapis": "178.0.0",
    "dotenv": "17.4.2",
    "@remotion/cli": "4.0.520",
    "@remotion/google-fonts": "4.0.520",
    "@remotion/renderer": "4.0.520",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "remotion": "4.0.520"
  }
}
